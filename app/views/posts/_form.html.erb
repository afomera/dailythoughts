<%= form_for(@post) do |f| %>
<% if @post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited this from being saved:</h2>

    <ul>
    <% @post.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class="form-group">
	<%#= f.text_area :body, :placeholder => "Post your thoughts here, markdown formatting supported", :rows => 10, class: "form-control" %>
  <%= f.trix_editor :body, :placeholder => "What's on your mind?", :rows => 10 %>
</div>

<div class="form-group">
  <%= f.submit "Save Post" %>
</div>

<% end %>
</div>
